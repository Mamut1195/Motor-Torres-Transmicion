# Input data scaffold only; normative load combinations remain blocked.

[[load_cases]]
id = "placeholder_service_load"
# Load provenance: explicit user input only. Normative self-weight, wind,
# conductor, combination, and factor generation remain TODO_DOMAIN_VALIDATION.
status = "explicit_user_input"
source = "explicit placeholder user input; not for final engineering design"

[[load_cases.nodal_loads]]
node_id = "top_node"
fx = { value = 0.0, unit = "kN" }
fy = { value = 0.0, unit = "kN" }
fz = { value = -1.0, unit = "kN" }
